var x = true ? true : false;
//
var x = true > 150 ? 1 : 5 / (1 + Math.exp(-0.1 * (rating.length - 50)));
//
var x = !y
    ? 0
    : visitCount > 160
    ? Math.log(visitCount) + 2.71
    : 5 / (1 + Math.exp(-0.05 * (visitCount - 80)));
